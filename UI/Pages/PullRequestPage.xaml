<Page x:Class="GitWave.UI.Pages.PullRequestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:GitWave.ViewModels"
      xmlns:controls="clr-namespace:GitWave.UI.Controls" 
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="800"
      Title="PullRequestPage" Background="#F3F3F3">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Margin="0,0,8,0">
            <DockPanel>
                <TextBlock Text="Open Pull Requests" DockPanel.Dock="Top" FontWeight="Bold" Margin="10"/>
                <Button DockPanel.Dock="Bottom" Margin="15" Height="32"
                        Command="{Binding CreatePrCommand}"
                        Background="#2da44e" Foreground="White" FontWeight="SemiBold"
                        FontSize="14" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="+" FontSize="18" FontWeight="Bold" Margin="0,-2,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="New Pull Request" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <ListView BorderThickness="0" Background="Transparent"
                          ItemsSource="{Binding PullRequests}"
                          SelectedItem="{Binding SelectedPullRequest}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E1E4E8"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F6F8FA"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="AliceBlue"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" 
                                           Text="{Binding Title, Mode=OneWay}" 
                                           FontWeight="SemiBold" 
                                           FontSize="14" 
                                           Foreground="#24292E"
                                           TextTrimming="CharacterEllipsis" 
                                           Margin="0,0,10,4"/>

                                <Border Grid.Row="0" Grid.Column="1" 
                                        VerticalAlignment="Top"
                                        CornerRadius="10" 
                                        BorderThickness="1"
                                        BorderBrush="{Binding StatusColor}"
                                        Padding="6,1" 
                                        Margin="0,0,0,0">
                                    <TextBlock Text="{Binding Status}" 
                                               Foreground="{Binding StatusColor}" 
                                               FontSize="10" 
                                               FontWeight="Bold"/>
                                </Border>

                                <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                           FontSize="12" 
                                           Foreground="#586069">
                                    <Run Text="#" Foreground="#586069"/>
                                    <Run Text="{Binding Number, Mode=OneWay}" Foreground="#586069"/>
                                    <Run Text="opened by"/>
                                    <Run Text="{Binding Author, Mode=OneWay}" FontWeight="Medium" Foreground="#24292E"/>
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Background="White" BorderBrush="#DDDDDD" BorderThickness="1"
                Visibility="{Binding IsDetailVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <controls:PullRequestDetailView DataContext="{Binding SelectedPullRequest}"/>
        </Border>
    </Grid>
</Page>