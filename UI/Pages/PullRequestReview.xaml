<Page x:Class="GitWave.UI.Pages.PullRequestReview"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PullRequestPage">

    <Page.Resources>
        <Style x:Key="SidebarHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#586069"/>
            <Setter Property="Margin" Value="0,16,0,8"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="TimelineItemStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1E4E8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Padding" Value="12"/>
        </Style>
    </Page.Resources>

    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" BorderBrush="#DDDDDD" BorderThickness="0,0,1,0">
            <DockPanel>
                <TextBlock Text="Pull Requests" DockPanel.Dock="Top" FontWeight="Bold" Margin="16"/>
                <Button DockPanel.Dock="Top" Margin="12,0,12,12" Height="32" Background="#2da44e" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Content="+ New PR">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <ListView BorderThickness="0" Background="Transparent" Margin="0,8,0,0">
                    <ListViewItem Padding="12,8" Background="#F0F8FF">
                        <StackPanel>
                            <TextBlock Text="Fix login validation" FontWeight="SemiBold"/>
                            <TextBlock Text="#42 • Open" FontSize="11" Foreground="Green"/>
                        </StackPanel>
                    </ListViewItem>
                    <ListViewItem Padding="12,8">
                        <StackPanel>
                            <TextBlock Text="Update test plan" FontWeight="SemiBold"/>
                            <TextBlock Text="#41 • Merged" FontSize="11" Foreground="Gray"/>
                        </StackPanel>
                    </ListViewItem>
                </ListView>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" Padding="24,20,24,0">
                <StackPanel>
                    <TextBlock Text="Fix login validation #42" FontSize="26" FontWeight="Normal" Foreground="#24292e"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,16">
                        <Border Background="#2da44e" CornerRadius="12" Padding="12,4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⭮" Foreground="White" Margin="0,0,6,0"/>
                                <TextBlock Text="Open" Foreground="White" FontWeight="Bold" FontSize="12"/>
                            </StackPanel>
                        </Border>
                        <TextBlock Text=" kaiwen " FontWeight="Bold" Foreground="#24292e" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        <TextBlock Text="wants to merge 2 commits into" VerticalAlignment="Center" Foreground="#586069"/>
                        <Border Background="#EAF5FF" CornerRadius="4" Padding="6,2" Margin="6,0">
                            <TextBlock Text="main" FontFamily="Consolas" Foreground="#0366d6"/>
                        </Border>
                        <TextBlock Text="from" VerticalAlignment="Center" Foreground="#586069"/>
                        <Border Background="#EAF5FF" CornerRadius="4" Padding="6,2" Margin="6,0">
                            <TextBlock Text="fix-login" FontFamily="Consolas" Foreground="#0366d6"/>
                        </Border>
                    </StackPanel>

                    <TabControl BorderThickness="0" Background="Transparent" Margin="-4,0,0,0">
                        <TabItem Header="Overview" IsSelected="True" FontSize="14" Padding="12,8"/>
                        <TabItem Header="Files Changed (2)" FontSize="14" Padding="12,8"/>
                        <TabItem Header="Commits" FontSize="14" Padding="12,8"/>
                    </TabControl>
                </StackPanel>
            </Border>

            <ScrollViewer Grid.Row="1" Background="White" VerticalScrollBarVisibility="Auto">
                <Grid Margin="24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">

                        <Border Style="{StaticResource TimelineItemStyle}">
                            <StackPanel>
                                <DockPanel LastChildFill="False" Margin="0,0,0,8">
                                    <TextBlock Text="kaiwen" FontWeight="Bold"/>
                                    <TextBlock Text="commented 2 days ago" Foreground="Gray" Margin="8,0,0,0"/>
                                    <Button DockPanel.Dock="Right" Content="..." Background="Transparent" BorderThickness="0" Foreground="Gray"/>
                                </DockPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="14" LineHeight="20" Foreground="#24292e">
                                    This PR fixes the timeout issue during login.<LineBreak/>
                                    I have updated the `StepSettings.config` to allow for a longer duration.
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <Grid Margin="20,10">
                            <Rectangle Height="1" Fill="#E1E4E8"/>
                            <Border Background="White" HorizontalAlignment="Center" Padding="10,0">
                                <TextBlock Text="3 commits" Foreground="Gray" FontSize="12"/>
                            </Border>
                        </Grid>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Ellipse Width="30" Height="30" Fill="#E1E4E8" Margin="0,0,12,0"/>
                            <TextBlock VerticalAlignment="Center">
                                <Run Text="Ali" FontWeight="Bold"/>
                                <Run Text="reviewed changes" Foreground="Gray"/>
                                <Run Text="yesterday" Foreground="Gray"/>
                            </TextBlock>
                        </StackPanel>

                        <Border Background="#F6F8FA" BorderBrush="#E1E4E8" BorderThickness="1" CornerRadius="6" Margin="0,0,0,24" Padding="12">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="✓" Foreground="Green" FontWeight="Bold" Margin="0,0,8,0"/>
                                    <TextBlock Text="All checks passed" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="24,4,0,0">
                                    <TextBlock Text="✓" Foreground="Green" Margin="0,0,6,0"/>
                                    <TextBlock Text="Build / Build &amp; Test" Foreground="Gray"/>
                                    <TextBlock Text="— Successful in 3m" Foreground="Gray" Margin="4,0,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border BorderBrush="#D1D5DA" BorderThickness="1" CornerRadius="6" Background="#F6F8FA">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" BorderBrush="#D1D5DA" BorderThickness="0,0,0,1" Background="#F6F8FA" Padding="10,8">
                                    <TextBlock Text="Add a comment" FontWeight="SemiBold"/>
                                </Border>

                                <TextBox Grid.Row="1" Height="100" Margin="10" BorderBrush="#D1D5DA" Padding="5" AcceptsReturn="True" VerticalContentAlignment="Top" Background="White"/>

                                <DockPanel Grid.Row="2" Margin="10,0,10,10" LastChildFill="False">
                                    <Button DockPanel.Dock="Left" Content="Close Pull Request" Background="Transparent" BorderThickness="1" BorderBrush="#D1D5DA" Padding="12,6" FontWeight="SemiBold" Foreground="#CB2431">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>

                                    <Button DockPanel.Dock="Right" Content="Submit Review" Background="#2da44e" Foreground="White" Padding="12,6" FontWeight="Bold" BorderThickness="0">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>

                                    <ComboBox DockPanel.Dock="Right" SelectedIndex="0" Margin="0,0,8,0" Width="140" Padding="6" VerticalContentAlignment="Center">
                                        <ComboBoxItem Content="Comment"/>
                                        <ComboBoxItem Content="Approve"/>
                                        <ComboBoxItem Content="Request Changes"/>
                                    </ComboBox>
                                </DockPanel>
                            </Grid>
                        </Border>

                    </StackPanel>

                    <StackPanel Grid.Column="2">

                        <TextBlock Text="Reviewers" Style="{StaticResource SidebarHeaderStyle}" Margin="0,0,0,8"/>
                        <WrapPanel>
                            <Border Background="#F0F0F0" CornerRadius="12" Padding="8,2" Margin="0,0,4,4">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="16" Height="16" Fill="Gray" Margin="0,0,6,0"/>
                                    <TextBlock Text="Ali"/>
                                </StackPanel>
                            </Border>
                            <TextBlock Text="+" Foreground="Blue" Cursor="Hand" VerticalAlignment="Center" Margin="4,0,0,0"/>
                        </WrapPanel>

                        <Border BorderBrush="#E1E4E8" BorderThickness="0,1,0,0" Margin="0,16,0,0"/>

                        <TextBlock Text="Assignees" Style="{StaticResource SidebarHeaderStyle}"/>
                        <TextBlock Text="No one — assign yourself" Foreground="Gray" FontStyle="Italic"/>

                        <Border BorderBrush="#E1E4E8" BorderThickness="0,1,0,0" Margin="0,16,0,0"/>

                        <TextBlock Text="Labels" Style="{StaticResource SidebarHeaderStyle}"/>
                        <WrapPanel>
                            <Border Background="#d4c5f9" CornerRadius="10" Padding="8,2" Margin="0,0,4,4">
                                <TextBlock Text="bug" FontSize="11" FontWeight="SemiBold"/>
                            </Border>
                            <Border Background="#c5def5" CornerRadius="10" Padding="8,2" Margin="0,0,4,4">
                                <TextBlock Text="frontend" FontSize="11" FontWeight="SemiBold"/>
                            </Border>
                        </WrapPanel>

                        <Border BorderBrush="#E1E4E8" BorderThickness="0,1,0,0" Margin="0,16,0,0"/>

                        <TextBlock Text="Milestone" Style="{StaticResource SidebarHeaderStyle}"/>
                        <TextBlock Text="No milestone" Foreground="Gray"/>

                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
