<UserControl x:Class="GitWave.UI.Controls.CreatePullRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitWave.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="New Pull Request" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Base (Target)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding Branches}" SelectedItem="{Binding SelectedTargetBranch}" Padding="6"/>
            </StackPanel>

            <TextBlock Grid.Column="1" Text="←" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Margin="0,18,0,0"/>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Compare (Source)" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding Branches}" SelectedItem="{Binding SelectedSourceBranch}" Padding="6"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Title" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Title}" Padding="5" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Body}" AcceptsReturn="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" Padding="5" Height="120"
                     VerticalContentAlignment="Top"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}" 
                    Padding="15,6" Margin="0,0,10,0" Background="Transparent"/>

            <Button Content="Create Pull Request" Command="{Binding CreateCommand}" 
                    Padding="15,6" Background="#2da44e" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>
